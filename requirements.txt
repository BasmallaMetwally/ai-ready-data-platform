# Unified Data Platform — merged requirements from all three original
# projects' Python-native parts (DQ system + e-commerce ETL/ML/API).
# The crypto pipeline's heavy optional stack (Airflow, Spark, dbt-postgres)
# is intentionally NOT in here — see ingestion/crypto/README.md.

# --- core / DQ system ---
pandas>=2.0,<3.0
numpy>=1.24,<3.0
matplotlib>=3.7,<4.0
jinja2>=3.0,<4.0

# --- ML (segmentation, forecasting, anomaly detection, recommendations) ---
scikit-learn>=1.3
scipy>=1.10
joblib>=1.3
pyarrow>=14.0   # optional but recommended: parquet history for forecasting

# --- unified API ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-multipart>=0.0.9
httpx2>=0.1   # required by starlette.testclient.TestClient as of Starlette
              # >=1.x — without it, `from fastapi.testclient import TestClient`
              # raises RuntimeError("...requires the httpx2 package...") in a
              # clean install. Confirmed by reproducing the failure in a fresh
              # venv with only this file installed, then fixing it with this
              # line and re-testing. Only needed for tests (TestClient); the
              # app itself doesn't import it.

# --- crypto ingestion adapter (ingestion/crypto) ---
requests>=2.31
pydantic-settings>=2.5

# --- tests ---
# (DQ + ETL + bridge tests use the stdlib unittest module — no extra deps.
#  The crypto pipeline's own test suite additionally wants pytest+responses;
#  see ingestion/crypto/README.md.)
